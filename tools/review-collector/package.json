{
  "name": "review-collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "bun run src/collect.ts",
    "filter": "bun run src/filter.ts",
    "embed": "bun run src/embed.ts",
    "build-cli": "bun build src/cli.ts --compile --outfile=../../bin/reviewer"
  },
  "dependencies": {
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
